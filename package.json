{
  "name": "srs-system",
  "version": "1.0.0",
  "description": "Student Registration System using Node.js, Express, and React",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "build": "cd client && npm install && npm run build",
    "postinstall": "cd client && npm install",
    "dev": "concurrently \"nodemon app.js\" \"cd client && npm run dev\""
  },
  "dependencies": {
    "express": "^4.18.2",
    "sqlite3": "^5.1.6",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "concurrently": "^8.2.2"
  }
}